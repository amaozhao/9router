# Postgres
DATABASE_URL=postgres://router:router_dev_pw@localhost:55432/router

# Redis
REDIS_URL=redis://localhost:56379/0

# Local crypto master key (dev only — replace with KMS in production)
# Generate: openssl rand -base64 32
CLOUD_MASTER_KEY=changeme-changeme-changeme-changeme-changeme=

# Router service
ROUTER_PORT=30100
ROUTER_LOG_LEVEL=info

# JWT signing for admin dashboard (Phase 2+)
JWT_SECRET=changeme-jwt-secret

# Optional providers — set if you want to test outbound calls
# OPENAI_API_KEY=
# GLM_API_KEY=
